# 实验日志

## 26.04.14
完成复现，代码可以跑通

## 26.04.15
### 测试指令
```bash
python test.py \
  --dataroot /root/autodl-tmp/pytorch-CycleGAN-and-pix2pix/datasets/archive/test/sketches \
  --name 3.UnetPP_test \
  --model test \
  --netG UnetPP \
  --norm batch \
  --direction AtoB \
  --dataset_mode single \
  --eval \
  --num_test 5000

python value.py \
  --name 3.ResUnet_test \
  --metrics psnr ssim lpips \
  --gt_dir /root/autodl-tmp/pytorch-CycleGAN-and-pix2pix/datasets/archive/test/photos \
  --pred_dir /root/autodl-tmp/pytorch-CycleGAN-and-pix2pix/results/3.ResUnet_test/test_latest/images \
  --allow_resize
```

尝试了以下代码
### 组别1
效果一般，人物如奶油般化开，训练时长3个小时
```bash
python train.py \
  --dataroot /root/autodl-tmp/pytorch-CycleGAN-and-pix2pix/datasets/my_dataset \
  --name baseline1 \
  --model pix2pix \
  --direction AtoB \
  --batch_size 16 \
  --num_threads 16 \
  --load_size 256 \
  --crop_size 256 \
  --netD n_layers \
  --n_layers_D 4 \
  --lambda_L1 70 \
  --print_freq 1000 \
  --save_latest_freq 20000 \
  --save_epoch_freq 10 \
  --n_epochs 100 \
  --n_epochs_decay 100
```

### 批量测试，寻找原因
**将lambda_L1: 70->50**
```bash
python train.py \
--dataroot /root/autodl-tmp/pytorch-CycleGAN-and-pix2pix/datasets/my_dataset \
--name ablate_A_l1_50 \
--model pix2pix \
--direction AtoB \
--batch_size 16 \
--num_threads 16 \
--load_size 256 \
--crop_size 256 \
--netD n_layers \
--n_layers_D 4 \
--lambda_L1 50 \
--gan_mode vanilla \
--print_freq 2000 \
--save_latest_freq 50000 \
--save_epoch_freq 6 \
--n_epochs 12 \
--n_epochs_decay 6
```

**修改GAN类型**
```bash
python train.py \
--dataroot /root/autodl-tmp/pytorch-CycleGAN-and-pix2pix/datasets/my_dataset \
--name ablate_B_lsgan \
--model pix2pix \
--direction AtoB \
--batch_size 16 \
--num_threads 16 \
--load_size 256 \
--crop_size 256 \
--netD n_layers \
--n_layers_D 4 \
--lambda_L1 70 \
--gan_mode lsgan \
--print_freq 2000 \
--save_latest_freq 50000 \
--save_epoch_freq 6 \
--n_epochs 12 \
--n_epochs_decay 6
```
```bash
python train.py \
--dataroot /root/autodl-tmp/pytorch-CycleGAN-and-pix2pix/datasets/my_dataset \
--name ablate_B_lsgan_upgrade \
--model pix2pix \
--direction AtoB \
--batch_size 16 \
--num_threads 16 \
--load_size 256 \
--crop_size 256 \
--netD n_layers \
--n_layers_D 4 \
--lambda_L1 60 \
--gan_mode lsgan \
--print_freq 2000 \
--save_latest_freq 50000 \
--save_epoch_freq 6 \
--n_epochs 12 \
--n_epochs_decay 6 \
--no_flip
```

**稍加增强判别器**
```bash
python train.py \
--dataroot /root/autodl-tmp/pytorch-CycleGAN-and-pix2pix/datasets/my_dataset \
--name ablate_C_D5 \
--model pix2pix \
--direction AtoB \
--batch_size 16 \
--num_threads 16 \
--load_size 256 \
--crop_size 256 \
--netD n_layers \
--n_layers_D 5 \
--lambda_L1 70 \
--gan_mode vanilla \
--print_freq 2000 \
--save_latest_freq 50000 \
--save_epoch_freq 6 \
--n_epochs 12 \
--n_epochs_decay 6
```

**增大分辨率，耗时更大**
```bash
python train.py \
  --dataroot /root/autodl-tmp/pytorch-CycleGAN-and-pix2pix/datasets/my_dataset \
  --name ablate_D_res512 \
  --model pix2pix \
  --direction AtoB \
  --batch_size 16 \
  --num_threads 16 \
  --load_size 572 \
  --crop_size 512 \
  --netD n_layers \
  --n_layers_D 4 \
  --lambda_L1 70 \
  --gan_mode vanilla \
  --print_freq 2000 \
  --save_latest_freq 50000 \
  --save_epoch_freq 6 \
  --n_epochs 12 \
  --n_epochs_decay 6
```

**集大成者**
```bash
python train.py \
  --dataroot /root/autodl-tmp/pytorch-CycleGAN-and-pix2pix/datasets/my_dataset \
  --name ablate_E_mixed \
  --model pix2pix \
  --direction AtoB \
  --batch_size 16 \
  --num_threads 16 \
  --load_size 512 \
  --crop_size 512 \
  --netD n_layers \
  --n_layers_D 5 \
  --lambda_L1 60 \
  --gan_mode lsgan \
  --print_freq 2000 \
  --save_latest_freq 50000 \
  --save_epoch_freq 6 \
  --n_epochs 12 \
  --n_epochs_decay 6 \
  --no_flip

python train.py \
  --dataroot /root/autodl-tmp/pytorch-CycleGAN-and-pix2pix/datasets/my_dataset \
  --name ablate_E_mixed_compared \
  --model pix2pix \
  --direction AtoB \
  --batch_size 16 \
  --num_threads 16 \
  --load_size 256 \
  --crop_size 256 \
  --netD n_layers \
  --n_layers_D 5 \
  --lambda_L1 60 \
  --gan_mode lsgan \
  --print_freq 2000 \
  --save_latest_freq 50000 \
  --save_epoch_freq 6 \
  --n_epochs 12 \
  --n_epochs_decay 6 \
  --no_flip

python test.py
  --dataroot /root/autodl-tmp/pytorch-CycleGAN-and-pix2pix/datasets/archive/test/sketches
  --name E_mixed
  --model test
  --netG unet_256
  --norm batch
  --direction AtoB
  --dataset_mode single
  --load_size 512
  --crop_size 512
  --preprocess none
  --num_test 50
  --eval
```

### 组别2
尝试更大的load_size和crop_size，看看能不能获得更好的效果
```bash
python train.py \
  --dataroot /root/autodl-tmp/pytorch-CycleGAN-and-pix2pix/datasets/my_dataset \
  --name baseline2 \
  --model pix2pix \
  --direction AtoB \
  --batch_size 8 \
  --num_threads 16 \
  --load_size 572 \
  --crop_size 512 \
  --netD n_layers \
  --n_layers_D 5 \
  --lambda_L1 50 \
  --print_freq 2000 \
  --save_latest_freq 20000 \
  --save_epoch_freq 10 \
  --n_epochs 60 \
  --n_epochs_decay 30
```

## 26.04.16
今天打算开始进行对比试验
使用tmux进行后台任务运行防止训练中断
```bash
# 1. 更新软件包列表（确保能装到最新版）
apt-get update

# 2. 安装 tmux
apt-get install -y tmux

# 3. 创建会话
tmux new -s <会话任务名>

# 4. 在新会话运行训练代码
python train.py

# 5. 退出当前会话
# 使用按键组合，ctrl+b，松开按d

# 6. 重新进入会话
tmux attach -t <会话任务名>

# 7. 训练完成后删除会话
tmux kill-session -t <会话任务名>
```
进行的基线设计：
```bash
python train.py \
  --dataroot /root/autodl-tmp/pytorch-CycleGAN-and-pix2pix/datasets/my_dataset \
  --name 3.UnetPP_test \
  --model pix2pix \
  --direction AtoB \
  --batch_size 8 \
  --num_threads 16 \
  --load_size 256 \
  --crop_size 256 \
  --netD n_layers \
  --netG UnetPP \
  --n_layers_D 4 \
  --lambda_L1 100 \
  --gan_mode lsgan \
  --print_freq 2000 \
  --save_latest_freq 50000 \
  --save_epoch_freq 10 \
  --n_epochs 15 \
  --n_epochs_decay 15 \
  --no_html \
  --lr 0.0002
```

--lr：0.0001 / 0.0002 / 0.0004
--crop_size & --load_size：256 / 384 / 512
--gan_mode：vanilla / lsgan / wgangp
但 wgangp 只有在把梯度惩罚真正接进 pix2pix 训练流程后，才适合作为正式实验项
--epoch：15+15 / 30+30 / 60+60
--lambda_L1：50 / 70 / 100
--n_layers_D: 3 / 4 / 5

数据增强方面

## 26.04.21
今天开始研究能不能替换U-Net结构

1. 命令行超参数入口（你自己的实验命令）
直接指定了生成器为 U-Net：
实验日志.md:13
实验日志.md:207
多组训练命令都在用 pix2pix：
实验日志.md:35
实验日志.md:293
2. 参数定义层（框架允许哪些 netG）
命令行参数定义了 netG，可选含 unet_256、unet_128：
`base_options.py:33`
3. 模型级默认覆盖（pix2pix 会把 netG 默认改成 unet_256）
pix2pix 修改默认参数：
`pix2pix_model.py:33`
4. 参数解析与模型创建链路（把超参数送到模型）
训练/测试脚本解析命令行参数：
`train.py:31`
`test.py:46`
根据 model 字段动态创建模型类：
`__init__.py:53`
`__init__.py:55`
5. 生成器构建入口（真正把 netG 传入网络工厂）
pix2pix 中把 opt.netG 传给 define_G：
`pix2pix_model.py:58`
网络工厂函数 define_G：
`networks.py:132`
6. U-Net 分支选择与实现
netG=unet_256 时分支到 UnetGenerator：
`networks.py:156`
`networks.py:157`
U-Net 主体类：
`networks.py:424`
U-Net 跳连子模块：
`networks.py:456`
7. 使用的深度学习库位置（你说的“使用库”）
使用 PyTorch 与 torch.nn：
`networks.py:1`
`networks.py:2`
U-Net 中核心层（Conv2d / ConvTranspose2d）：
`networks.py:483`
`networks.py:490`
8. 运行结果记录（证明实际跑的也是 U-Net）
你当前实验配置文件记录 netG: unet_256：
`train_opt.txt:33`

可直接作为生成器思路的：
unet++
resunet
需要改造后才能用于 pix2pix 生成器的：
vgg
densenet
efficientnet
mobilenet
fpn

已经添加了ResUnet，可以使用参数`--netG ResUnet`使用，也可以`--netG ResUnet_256`、`--netG ResUnet_b4`、`--netG ResUnet_256_b4`等自由搭配
已经添加了Unet++，可以使用参数`--netG UnetPP`使用，也可以`--netG UnetPP_256`、`--netG UnetPP_d4`、`--netG UnetPP_256_d4`等自由搭配